<%= form_for(@bolet) do |f| %>
  <% if @bolet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bolet.errors.count, "error") %> prohibited this bolet from being saved:</h2>

      <ul>
      <% @bolet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div align="center" >
  <div align="left" class="field">
    <%= f.label :numboleta %><br>
    <%= f.number_field :numboleta %>
  </div>
  <div align="left" class="field">
    <%= f.label :fecha %><br>
    <%= f.date_select :fecha %>
  </div>
  <div align="left" class="field">
    <%= f.label :cliente_id %><br>
    <%= f.select( "cliente_id",Cliente.all.collect{|l| [ l.nombre, l.id ] }, {include_blank: 'Seleccione nombre'})  %>

  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
